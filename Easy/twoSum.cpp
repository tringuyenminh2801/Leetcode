#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        vector<int> result;
        int max_index = nums.size() - 1;
        int i = 0;
        while (i < max_index){
            
        }
        return result;
    }
};